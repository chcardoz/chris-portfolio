---
title: 'Gaussian Processes are all you need'
publishedAt: '2026-03-21'
summary: 'Will fill this out later'
draft: true
---

We are told that neural networks are modeled after the brain. Specifically, the fundamental unit of the neural network (perceptron) is modeled after the fundamental unit of your brain (neuron). If you look closely at a neuron, the presence of ____ induces a action potentional. When crossing a threshold, this potential allows ____ to flow through the network into its neighboring neurons. Perceptrons are the abstract model of this firing behavior. 

___ Simulate a neuron with a action potential wiht inputs and output ____

Just liek a neurons we have a inputs, each of those inputs are weighed by a number and if there is a enough of the inputs, we send out a ouptut. this can be repesented by a action potential function (lets call it ). 

You can also show this mathematically, and its not that crazy. When you have a single neuron, you take the inputs, you take the weights, the bias and then out comes a weighted sum. 

---- show the diagrma fo the inputs comming in and ____

now we have to show the equation here. 

You might have seen this formula somewhere else. Its actually the regression curve? is that right? A single perception is just a way to fit a curve. Try to tune the weights and bias to fit the curve below

__ another simulation __

When we add a few a more neurons and connections, things get a little bit murkier. But lets take it step by step. If we follow one neuron at a time, we can use the weighted sum just as before. The neurons in the start dont have any connections coming in so we can just label their values as in 1 and in 2. always towards the end, you only have a neuron with no connections going out which we can call out 1. the neurons int eh middle are the most interesting and I will call them hidden 1 and hidden 2. the formual for what the value of hidden one is this and for hidden two is this. and the formula for what the vlaue of out 1 is jsut this. 

-- we hav eto show the equaiotns here --

But in the out 1, we can substitute the values of hidden 1 and hidden 2 for the ones we calculated and we have the value of out 1 represented by the what the values of hte in 1 and in 2 neurons are. This representation of the final neuron in terms of the starting neurons gets more and more complex the more neurons we add. But lets focus only on this simple configuraiton. We can change the value of the out 1 neuron by changing either the input neurons or any of the weights and biases. You must be asking why cant I change the hidden neurons. Well they can be represented fully by the neurons before them and hence its not required to touch them directly. 

Now I am going to do somethign fun, I am going to roll a die and then pcik out the weights and biases (or sometimes caled parameters) of these network. And then I will lock them in place. Then I will change the input 1 and input 2 neuron and then see what happens to the output. Try it for yourself here. 

--- a simulation of locked weights and biases and then alloweing the uses to change the input and seeing the output change --

Since we cannot change the hidden neurons, I will not even care to to you show them, hidig them in a box. Essentially what we are seeing here is that neural networks behave just like a function with two inputs and one output. Now I will switch out the box for a function like f(a,b) = a^3b^2. Move the inputs around and see the output change

--- same sim as locked wieghts and biases but now its a function thats predefined --

This shoudl be that surprising since we can actually represent mathematically our simple network as a function. Like what we did above. in 1 and in 2 are a and b and then f(a,b) is out 1. Just in this case it does not look as nice as a function you are used to (hitn: its becuase of that activation function).

{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter @cbrain/site dev",
    "build": "bun --filter @cbrain/site build",
    "start": "bun --filter @cbrain/site start",
    "brain": "bun --filter @cbrain/cli brain",
    "mcp": "bun --filter @cbrain/mcp mcp",
    "typecheck": "bun --filter '*' typecheck",
    "lint": "bun --filter '*' lint",
    "test": "bun --filter '*' test",
    "format": "bunx prettier --check package.json tsconfig.base.json README.md CLAUDE.md && bun --filter '*' format"
  }
}

{
  "scripts": {
    "setup": "bun install && [ -f \"$CONDUCTOR_ROOT_PATH/.env\" ] && ln -sf \"$CONDUCTOR_ROOT_PATH/.env\" .env || echo 'No .env found in repo root — copy .env.example to .env and fill in values'",
    "run": "bun run dev",
    "archive": "rm -rf node_modules .next"
  }
}
